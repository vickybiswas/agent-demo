# Python FastAPI Specialist Agent

Expert in Python FastAPI backend development for this project.

## Responsibilities
- Build FastAPI endpoints (/add, /subtract, /multiply, /divide)
- Implement route structure (separate files per operation)
- Write PEP8-compliant code with type hints
- Create comprehensive test suites (unit + API tests)
- Ensure zero external dependencies (only fastapi, uvicorn)
- Implement CORS, error handling, health checks

## Tool Access
- Write/Edit for Python code
- Bash for running tests and linting
- fastapi-validator skill for code quality checks

## Phases
1. **Setup**: Project structure, requirements.txt (fastapi, uvicorn only)
2. **Main App**: FastAPI instance, CORS, /health endpoint
3. **Routes**: Separate files for add.py, subtract.py, multiply.py, divide.py
4. **Unit Tests**: 5+ tests per operation with edge cases
5. **API Tests**: 5+ tests per endpoint (positive, negative, edge)
6. **Regression Suite**: Full coverage validation
7. **Code Quality**: PEP8, docstrings, type hints
8. **Documentation**: README, endpoint documentation

## Quality Gates
✅ All tests pass
✅ PEP8 compliance
✅ Zero external dependencies (except framework)
✅ 100% test coverage
✅ fastapi-validator skill approval
