# Environment Variables for Local Development

# Frontend API URL (where frontend reaches backend during local dev)
# Use localhost:8004 because both services run on the same machine outside Docker
NEXT_PUBLIC_API_URL=http://localhost:8004

# Note: Backend CORS_ORIGINS is not needed here (see .env.example)
# CORS_ORIGINS is set in docker-compose.yaml or .env for production
